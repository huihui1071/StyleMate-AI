# AI 导购顾问 GitHub 项目计划

> 项目定位：一个可公开展示、可本地运行、可评测的时尚零售 AI 员工 Demo。  
> 暂定项目名：`StyleMate AI`  
> 核心能力：智能导购、搭配顾问、会员顾问、订阅服务推荐、人工接管。  
> 发布原则：项目中不得出现真实企业名、真实品牌名、真实会员信息、内部图片域名或内部资料原文。

## 1. 资料分析结论

本计划基于商品资料、搭配图片资料、会员样例、订阅规则和 AI 测试标准完成分析。源资料只用于需求提炼，不复制到公开仓库。

### 1.1 商品与搭配资料

- 商品库共 3,235 条颜色级商品记录，对应 1,875 个款号。
- 商品字段已覆盖品牌、款号、商品名、图片、价格、工艺、面料、颜色、体型、合体度、品类、季节、风格和卖点，足以支持结构化检索与推荐解释。
- 商品分属 4 个品牌线、22 个品类、2 个季节、18 个风格标签；公开 Demo 中统一替换为 `Brand_A` 至 `Brand_D` 等虚构标识。
- 价格范围为 195–12,995，中位数为 1,195，可支持预算过滤、价格排序和整套预算控制。
- 主要缺失项：工艺约 25.5%、风格标签约 12.7%、卖点描述约 12.0%、商品图约 2.9%。系统必须明确区分“未知”与“不满足”，不得用模型补造事实。
- 搭配素材共 2,377 组，包含 5,280 个去重商品编号；与当前商品库可直接匹配 496 个商品，约占当前商品库 15.3%。因此搭配素材只能作为高质量加分项，不能成为所有搭配推荐的前置条件。

### 1.2 会员资料

- 样例包含 10 位虚拟会员、31 条品牌维度记录。
- 同一会员可能跨多个品牌线拥有不同积分、等级、购买历史与偏好，导入时必须先按会员编号聚合，再保留品牌维度明细。
- 可用个性化信号包括：历史购买款、会员等级、积分、订阅状态、当前发盒节点、兑换值、版型偏好和季节偏好。
- 公开 Demo 仅保留重新生成的虚构会员；不提交源会员编号、购买记录或任何可关联信息。

### 1.3 订阅服务规则

- 一年 6 个发盒节点，用户开通后选择月份；节点到达时提醒、发盒、试穿购买、预约退回。
- 发盒提供双向包邮；第 1、3、6 个盒子具有收盒礼。
- 首盒包含两张 100 元购衣券；订阅专属兑换值达到 4,000 后可兑换礼品。
- 金卡门槛：3,888 积分或 199 元；银卡门槛：3,990 积分或 399 元。Demo 中等级改为虚构的 `Tier_Gold`、`Tier_Silver`。
- 新订阅用户开启自动续订：当次积分价 5 折、下周期 9 折；已订阅用户开启自动续订：下周期 5 折。
- “当次 5 折是否只适用于积分支付”在资料中表达较明确但仍应配置化；代码中不把促销文案硬编码进提示词。

### 1.4 测试标准提炼

- 单次推荐数量不得超过 5–8 款。
- 每个推荐结果应尽量包含：商品图、名称或款号、价格、面料/版型/工艺、搭配素材图。
- 必测能力：单意图、多意图、组合筛选、口语化场景、身材与风格理解、多轮记忆、指代消解、折扣计算、订阅推荐、自动标签、无资料时转人工、指定款号搭配、已有单品搭配、场景整套搭配、不同会员差异化推荐、搭配理由和真人化表达。

## 2. 产品范围

### 2.1 智能导购

输入可以是自然语言或筛选控件，例如：“想找一件适合春季通勤的灰色宽松外套，预算 1,500 以内。”

处理流程：

1. 将自然语言解析为结构化条件：品牌、品类、颜色、版型、材质、预算、季节、场景、风格及软硬约束。
2. 硬条件用数据库过滤，确保结果不混入不满足项。
3. 软条件使用文本向量和规则分数重排。
4. 返回不超过 8 款，并逐项说明命中条件和可能的取舍。
5. 无精确结果时先征求用户是否放宽某一个条件，不擅自放宽。

### 2.2 搭配顾问

支持三种入口：

- 指定商品编号：围绕某件商品找上装、下装、外套或鞋配。
- 描述已有单品：例如“我有一条米白色亚麻阔腿裤”。
- 描述场景：例如通勤、旅行、约会、婚礼、周末休闲。

每套搭配输出：主单品、2–4 件搭配单品、整套价格、风格说明、搭配理由、颜色关系、材质/廓形关系以及素材图（存在时）。若没有可关联街拍图，明确标记为“基于属性生成的搭配”，不伪造素材。

### 2.3 会员顾问

会员顾问先读取已授权的会员上下文，再决定是否个性化：

- 根据历史购买、版型偏好、季节偏好和品牌偏好调整排序。
- 显示积分、等级、订阅状态、节点与兑换值，但不在未认证会话中泄露会员信息。
- 只在合适时机软性介绍 `BOX+`：用户有明确购物兴趣、满足门槛、询问权益，或当前处于需要提醒的节点。
- 同一会话默认最多主动推荐一次；用户拒绝后停止追问。
- 触发节点提醒、兑换提醒、到期续订提醒，并清楚说明规则依据。

### 2.4 人工接管

以下情况直接建议转人工，并携带结构化摘要：

- 商品库没有答案，或关键字段缺失且会影响决策。
- 用户询问实时库存、门店库存、最终活动价，但 Demo 没有实时系统。
- 需要支付、退款、修改订阅、预约退回等真实操作。
- 会员身份未验证、资料冲突或涉及隐私申诉。
- 连续两次澄清后仍无法确认需求，或模型置信度低于阈值。

人工接管摘要包含：用户目标、已确认条件、候选商品、会员上下文、未解决问题和触发原因。

## 3. 公开仓库的数据匿名化方案

### 3.1 禁止进入 Git 的内容

- 原始 PDF、Excel、商品图、街拍图和下载缓存。
- 真实企业名、真实品牌名、真实商品编号、会员编号、内部域名、内部组织名称及原始宣传口号。
- 标注为保密或内部使用的原文、截图和规则文件。

### 3.2 可公开的替代数据

- 生成 150–300 条合成商品，保留字段分布和业务关系，不保留逐行映射。
- 使用新生成的 `SKU-DEMO-*`、`STYLE-DEMO-*` 编号。
- 用 `Brand_A` 至 `Brand_D` 或完全虚构的品牌名。
- 使用 AI 生成或许可证明确的演示图片；默认仓库可先使用抽象占位图。
- 生成 8–12 位虚构会员画像，覆盖订阅中/未订阅、不同积分、不同节点和偏好。
- 将订阅规则写成独立 JSON 配置，并在 README 标注为“演示业务规则”。

### 3.3 发布前自动门禁

新增 `scripts/privacy_scan.py`，在 CI 中扫描：

- 禁词表；
- 源资料文件名；
- 内部域名与图片 URL；
- 疑似真实商品编号格式；
- Excel、PDF 和大体积二进制文件。

扫描不通过时阻止提交或 CI 发布。`.gitignore` 明确忽略 `data/raw_private/`、`*.xlsx`、`*.pdf` 和图片下载目录。

## 4. 技术方案

### 4.1 推荐技术栈

- Web：Next.js + TypeScript + Tailwind CSS。
- API：FastAPI + Pydantic。
- 数据库：本地 Demo 使用 SQLite；提供 PostgreSQL 适配说明。
- 检索：SQLite 结构化查询 + FTS5；可选向量重排，不让向量检索替代硬过滤。
- LLM：OpenAI-compatible 接口，通过环境变量配置；同时提供无 Key 的规则化演示模式。
- 测试：Pytest、Playwright、固定评测集与离线评分脚本。
- 部署：Docker Compose；前后端也可分别部署到常见托管平台。

### 4.2 总体流程

```text
用户输入
  -> 意图识别与实体抽取
  -> 会话状态/会员授权上下文
  -> 路由器
       -> 商品筛选服务（硬条件）
       -> 推荐重排服务（软条件与会员偏好）
       -> 搭配服务（搭配图关系 + 属性规则）
       -> 订阅规则引擎（确定性规则）
       -> 人工接管判断
  -> 事实校验与引用商品字段
  -> 卡片化回答 + 推荐理由
```

关键设计：LLM 只负责理解、编排和表达；价格、门槛、节点、过滤结果与折扣全部由工具或规则引擎计算。

### 4.3 核心数据表

- `products`：款号、颜色级 SKU、品牌、品类、颜色、材质、价格、版型、合体度、季节、风格、工艺、卖点、图片。
- `looks`：搭配图、场景、风格和说明。
- `look_items`：搭配图与多个商品的多对多关系。
- `members`：虚构会员基本信息、授权状态、订阅状态、节点、兑换值。
- `member_brand_profiles`：品牌线积分、等级、购买历史和偏好。
- `subscription_rules`：门槛、权益、折扣、提醒节点和生效时间。
- `conversation_state`：已确认条件、候选列表、指代映射、已拒绝推荐和接管状态。
- `handoff_events`：转人工原因与会话摘要。

### 4.4 推荐评分

先执行硬过滤：

```text
品牌 AND 品类 AND 价格区间 AND 明确颜色/材质/版型
```

再对候选集重排：

```text
总分 = 0.30 场景风格匹配
     + 0.25 会员偏好匹配
     + 0.20 搭配关系强度
     + 0.15 季节匹配
     + 0.10 信息完整度
```

权重放在配置文件中。新用户没有会员数据时自动重新归一化；缺失字段不得视为匹配。

## 5. 对话与规则设计

### 5.1 意图结构

```json
{
  "intents": ["product_search", "outfit_advice"],
  "filters": {
    "brand": [],
    "category": [],
    "color": [],
    "fit": [],
    "material": [],
    "price_min": null,
    "price_max": null,
    "season": [],
    "style": [],
    "scenario": []
  },
  "hard_constraints": [],
  "soft_preferences": [],
  "needs_clarification": false
}
```

一句话可包含多个意图；路由器分别执行后合并回答。会话状态保存候选商品顺序，以正确理解“第二款”“换成深色”“预算再低一点”。

### 5.2 `BOX+` 推荐时机

| 状态 | 系统行为 |
|---|---|
| 未订阅、积分达到对应等级门槛、已有明确购物意图 | 推荐积分开通，并简述最相关的 2–3 项权益 |
| 未订阅但不满足积分门槛 | 仅在用户主动询问时展示现金方案，不制造紧迫感 |
| 订阅中且到第 1/3/6 个节点 | 提醒发盒与收盒礼 |
| 订阅中且兑换值 ≥ 4,000 | 提醒可兑换，不虚构具体库存礼品 |
| 即将到期 | 提醒续订或自动续订规则 |
| 用户拒绝、投诉或正在处理售后 | 不营销，优先解决问题或转人工 |

### 5.3 防幻觉规则

- 所有商品事实都必须来自检索结果的结构化字段。
- 价格卡片显示数据时间与“标准价/演示价”标签。
- 没有活动、库存、尺寸或成分信息时直接说明未知。
- 计算类回答返回规则引擎的计算过程，不让 LLM 心算。
- 每条推荐附 `evidence_fields`，便于评测命中依据。

## 6. GitHub 仓库结构

```text
stylemate-ai/
├── README.md
├── LICENSE
├── .env.example
├── .gitignore
├── docker-compose.yml
├── apps/
│   ├── web/
│   └── api/
├── packages/
│   ├── schemas/
│   └── ui/
├── data/
│   ├── demo/products.jsonl
│   ├── demo/looks.jsonl
│   ├── demo/members.json
│   └── demo/subscription_rules.json
├── scripts/
│   ├── generate_demo_data.py
│   ├── seed_db.py
│   └── privacy_scan.py
├── evals/
│   ├── cases.jsonl
│   ├── expected_constraints.json
│   └── run_evals.py
├── tests/
│   ├── unit/
│   ├── integration/
│   └── e2e/
└── docs/
    ├── architecture.md
    ├── data-model.md
    └── privacy.md
```

## 7. 实施阶段

### 阶段 0：匿名化与项目脚手架（0.5–1 天）

- 建立 monorepo、许可证、环境变量模板和 Docker。
- 创建禁止提交清单、隐私扫描脚本与 CI 门禁。
- 生成首批虚构商品、搭配和会员数据。

验收：仓库全文扫描不含真实企业/品牌/内部域名；新环境可一条命令启动。

### 阶段 1：智能导购 MVP（1.5–2 天）

- 完成数据模型、导入脚本与结构化检索 API。
- 完成意图解析、组合筛选、候选重排和商品卡片。
- 支持多轮修改条件和“第 N 款”指代。

验收：所有硬条件零违规；无结果时不返回伪匹配；单次最多 8 款。

### 阶段 2：搭配顾问（1.5–2 天）

- 建立搭配图关系和属性搭配规则。
- 支持指定商品、已有单品和场景三种入口。
- 输出整套预算、风格说明和逐项搭配理由。

验收：每套 3–5 件；互斥品类和预算约束可验证；素材缺失时正确降级。

### 阶段 3：会员与订阅顾问（1–1.5 天）

- 完成会员授权态、画像聚合和个性化重排。
- 完成订阅规则引擎、节点提醒、兑换提醒、续订与抑制营销规则。
- 完成人工接管摘要。

验收：相同需求对不同画像产生可解释差异；所有权益和折扣由规则引擎计算；拒绝后不重复营销。

### 阶段 4：评测、体验与发布（1–1.5 天）

- 建立固定评测集、单元测试、集成测试和浏览器端到端测试。
- 优化移动端聊天界面、筛选器、商品卡和“为什么推荐”抽屉。
- 增加架构图、演示 GIF、示例对话、技术决策和限制说明。

验收：CI 全绿；README 可复现；公开 Demo 不依赖私有数据；失败场景有清晰反馈。

预计总工期：5.5–7 个专注工作日。

## 8. 评测计划

### 8.1 固定测试集

至少建立以下 16 组测试：

1. 单品类筛选。
2. 品牌 + 品类 + 颜色 + 预算组合筛选。
3. 口语化通勤需求。
4. 度假、婚礼等场景理解。
5. 一句话同时选款和搭配。
6. 多轮缩小预算。
7. “第二款”指代。
8. 无精确结果时询问放宽条件。
9. 指定商品编号搭配。
10. 描述已有单品搭配。
11. 场景整套搭配。
12. 不同会员对相同需求的差异化结果。
13. 积分达标时介绍订阅。
14. 订阅节点/兑换值提醒。
15. 用户拒绝后停止营销。
16. 库中无答案、实时库存或售后请求转人工。

### 8.2 量化指标

- 硬条件准确率：100%。
- 商品事实引用准确率：100%。
- 推荐数量合规率：100%。
- 无资料时拒答/转人工召回率：≥ 95%。
- 多轮指代正确率：≥ 90%。
- 搭配完整率：≥ 95%。
- 订阅推荐时机准确率：≥ 90%。
- 会员差异化：Top 5 结果或排序存在合理且可解释差异。
- P95 API 延迟：规则模式 < 800 ms；LLM 模式 < 5 s。

## 9. Demo 页面设计

- 左侧：聊天记录与快捷问题。
- 中间：对话输入、澄清问题和商品/搭配卡片。
- 右侧：当前识别条件、会员画像、订阅状态和规则依据，可随时折叠。
- 商品卡：图片、虚构品牌、名称、SKU、价格、材质、版型、命中标签和“加入搭配”。
- 搭配卡：整套视觉、单品清单、总价、搭配理由和素材来源类型。
- 人工接管：展示可编辑摘要和“模拟转人工”状态，不执行真实外部操作。

## 10. 风险与处理

| 风险 | 处理方式 |
|---|---|
| 源资料带有保密标记 | 不复制、不截图、不提交；只保留抽象后的字段与合成数据 |
| 品牌和商品编号可反推企业 | 全部换成虚构名称和全新编号，且不保存逐行映射 |
| 搭配图覆盖低 | 采用“真实关联优先、属性规则降级”，并明确标注来源类型 |
| 部分商品字段缺失 | 缺失值降权并显示未知，不让 LLM 补造 |
| 促销规则变动 | 独立配置、带生效时间和版本号，不硬编码在 Prompt |
| 会员隐私 | 默认虚构会员；未授权不加载画像；日志脱敏 |
| 推荐看似合理但违反硬条件 | 数据库先过滤、结果后校验，LLM 不直接决定商品集合 |

## 11. 最终交付物

- 可公开的 GitHub 仓库与完整 README。
- 可本地运行的 Web Demo 和 Docker 启动方式。
- 合成商品、搭配、会员和订阅规则数据。
- 智能导购、搭配顾问、会员顾问与人工接管四条完整链路。
- 固定评测集、自动评分报告和 CI。
- 架构、数据模型、隐私与限制文档。
- 演示截图/GIF，以及 3–5 分钟项目讲解脚本。

## 12. 建议的第一版边界

第一版不连接真实库存、支付、CRM、客服或消息系统，不声称价格与库存实时有效；不执行真实开通、续订、退货或积分兑换。它应重点展示：可靠的约束检索、可解释搭配、会员个性化、确定性规则调用、隐私意识和可评测性。这些能力比“只做一个聊天框”更适合作为求职 GitHub 项目的核心亮点。
